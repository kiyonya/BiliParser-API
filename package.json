{
  "name": "cloudflare-bili-api",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "check-cons": "node check-consistency.mjs",
    "typegen":"wrangler types && cd entry-cache && wrangler types",
    "typegen-main": "wrangler types",
    "typegen-entry": "cd entry-cache && wrangler types",
    "deploy-entry": "cd entry-cache && wrangler deploy",
    "dev-entry": "cd entry-cache && wrangler dev",
    "dev-main": "wrangler dev",
    "deploy-main": "wrangler deploy",
    "deploy": "wrangler deploy && cd entry-cache && wrangler deploy"
  },
  "dependencies": {
    "@types/xml2js": "^0.4.14",
    "chanfana": "^3.3.0",
    "hono": "^4.12.12",
    "lru-cache": "^11.5.1",
    "xml2js": "^0.6.2",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "wrangler": "^4.107.0"
  }
}